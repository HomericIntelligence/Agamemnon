[workspace]
name = "agamemnon"
version = "0.1.0"
description = "Planning, coordination, and agentic orchestration for HomericIntelligence"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
cmake = ">=3.20"
ninja = ">=1.11"
cxx-compiler = ">=1.7"
conan = ">=2.0"
openssl = ">=3"
clang-tools = ">=17"
gcovr = ">=7"
pre-commit = ">=3"
libcurl = ">=8.20.0,<9"
actionlint = ">=1.6"

[tasks]
deps = "conan install . --output-folder=build/debug --profile=conan/profiles/debug --build=missing"
build = { cmd = "cmake --preset debug -DCMAKE_C_COMPILER=/usr/bin/gcc -DCMAKE_CXX_COMPILER=/usr/bin/c++ -DAgamemnon_ENABLE_CLANG_TIDY=OFF && cmake --build --preset debug", depends-on = ["deps"] }
test = "ctest --preset debug --output-on-failure && python -m pytest clients/python/tests/test_agents_md_consistency.py -v"
lint = "./scripts/lint.sh"
format = "./scripts/format.sh"
actionlint = "actionlint"
ci-smoke = "python -m pytest clients/python/tests/test_ci_workflows.py -v"
coverage = "conan install . --output-folder=build/coverage --profile=conan/profiles/debug --build=missing && cmake --preset coverage && cmake --build --preset coverage && ./scripts/coverage.sh"
